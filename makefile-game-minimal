# Minimal makefile, only suited for projects with a single c file

all:
	v810-gcc *.c -o main.elf -Wall -nodefaultlibs -mv810 -T$(ENGINE_FOLDER)/compiler/vb.ld -I$(ENGINE_FOLDER) -B$(BASE_FOLDER)/gcc/lib/
	v810-objcopy -O binary main.elf output.vb
